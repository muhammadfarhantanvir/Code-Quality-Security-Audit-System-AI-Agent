services:
  - type: web
    name: code-audit-ai-backend
    env: docker
    rootDir: backend
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: code-audit-db
          property: connectionString
      - key: RENDER
        value: "true"
      - key: PORT
        value: "10000"
    healthCheckPath: /

databases:
  - name: code-audit-db
    plan: free
